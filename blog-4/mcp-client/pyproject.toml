[project]
name = "mcp-client"
version = "0.1.0"
description = "Multi-provider CLI MCP host from Blog 4 of the MCP series"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.79.0",
    "google-genai>=1.0.0",
    "mcp>=1.26.0",
    "openai>=1.60.0",
    "python-dotenv>=1.2.1",
]
