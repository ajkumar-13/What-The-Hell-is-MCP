# Normalize line endings: LF in the repository, native in the working copy.
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.pdf binary

# SVGs are text and should diff as text
*.svg text eol=lf diff
