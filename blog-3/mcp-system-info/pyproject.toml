[project]
name = "mcp-system-info"
version = "0.1.0"
description = "System information MCP server from Blog 3 of the MCP series"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.26.0",
    "psutil>=7.2.2",
]
